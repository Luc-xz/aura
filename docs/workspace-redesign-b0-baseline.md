# 工作台改造 B0 验收记录

日期：2026-09-23。基准：`fa4c189`。范围仅为测试环境、测试契约及文档。

## 历史核对

执行计划原写“67f0847 撤出的 190 行、12+2=14 例”。实际从本地 reflog 的
`a0eec5c` 找到这 190 行：workspace 10 条项目字段 + 3 条统计，chat 2 条回退，
共 **15 条**。完整恢复，没有为凑数删除测试。

恢复时修复了原统计 fixture 使用 `pool` 却没有导入的问题，并为默认模型设置添加
HTTP 成功断言，避免把前置步骤失败误报为聊天失败。状态错误文案只要求指出 status，
不固定英文整句。没有修改业务代码。

## 环境

- 本机 Node 24.13.0、Vitest 4.0.18、MySQL 9.7.1、Redis 7.2.8。
- 专用 MySQL `127.0.0.1:13306/aura_test`，测试账号仅获该库权限。
- 专用 Redis `127.0.0.1:16379`；数据位于工作区 `.test-services.local`。
- 凭据只写入忽略文件；未读取或执行开发库迁移。
- 先核对数据库实际数据目录，再执行测试。
- 已验证专用服务停止后，通过 `start-test-services.ps1` 重新启动成功。
- 配置校验检查：缺少配置、未确认重置、缺少 Redis 凭据、非法端口会拒绝运行；
  合法测试配置覆盖进程中的业务连接设置。共 5 组断言通过。

## 实测结果

| 检查 | 总数 | 通过 | 失败 | 跳过 |
| --- | ---: | ---: | ---: | ---: |
| 恢复前 workspace/chat | 16 | 16 | 0 | 0 |
| 恢复前全量 | 131 | 131 | 0 | 0 |
| 恢复后全量 | 146 | 133 | 13 | 0 |

恢复后原有 131 条均通过。新增 15 条中，空更新返回 400、统计未认证返回 401
这 2 条现有行为已满足；其余 13 条是未实现功能的预期红灯。

## 红灯归因

| 契约 | 数量 | 当前失败 | 后续阶段 |
| --- | ---: | --- | --- |
| 创建/读取项目字段、默认状态 | 3 | 响应没有 goal/description/status | B1/B2 |
| 非法状态及字段长度 | 3 | 应为 400，实际为 200 | B2 |
| 更新归档状态、清空 goal | 2 | 更新被拒绝或字段不存在 | B2 |
| 按状态筛选 | 1 | 仍返回全部项目 | B2 |
| 项目统计及空统计 | 2 | 路由不存在，返回 404 | B2 |
| 创建/聊天默认模型回退 | 2 | PUT /user/settings 未实现，落入现有动态路由返回 403 | B3/B4 |

## 结论

**B0 验收通过，可以提交并开始 B1/B2；G1 尚未达成。**

原始 JSON 和日志保留在本机 `.test-services.local/`：
`baseline-targeted.json`、`baseline-full.json`、`b0-restored-full.json` 及对应日志。
它们不是长期版本化产物。手动运行说明见 `server/test/README.md`。

B0 不新增统计/详情/设置的完整用例矩阵；按后续阶段逐项补充，并在实现前观察红灯。
